{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck:api-server:clean": "rm -rf lib/db/dist lib/db/tsconfig.tsbuildinfo lib/api-zod/dist lib/api-zod/tsconfig.tsbuildinfo artifacts/api-server/.tsbuildinfo && pnpm --filter @workspace/api-server run typecheck",
    "typecheck:frontend": "pnpm --filter @workspace/wudooh --filter @workspace/mockup-sandbox run typecheck",
    "test:auth-security": "pnpm --filter @workspace/api-server run test:auth-security",
    "test:password-reset-delivery": "pnpm --filter @workspace/api-server run test:password-reset-delivery",
    "test:billing": "pnpm --filter @workspace/api-server run test:billing",
    "test:stripe-webhook-security": "pnpm --filter @workspace/api-server run test:stripe-webhook-security",
    "test:inventory": "pnpm --filter @workspace/api-server run test:inventory",
    "test:platform-admin": "pnpm --filter @workspace/api-server run test:platform-admin",
    "verify:ci-check-names": "pnpm --filter @workspace/scripts run verify:ci-check-names",
    "verify:branch-protection": "pnpm --filter @workspace/scripts run verify:branch-protection",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "stripe": "^22.5.0",
    "stripe-replit-sync": "^1.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
