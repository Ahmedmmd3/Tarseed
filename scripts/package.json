{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "test": "tsx --test ./src/*.test.ts",
    "seed:wudooh-plans": "tsx ./src/seed-wudooh-plans.ts",
    "verify:ci-check-names": "tsx ./src/verify-ci-check-names.ts",
    "verify:branch-protection": "tsx ./src/verify-branch-protection.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
